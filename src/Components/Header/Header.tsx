import { TopBanner } from './TopBanner';
import { Navbar } from './Navbar';

export const Header = () => {
  return (
    <header>
      <TopBanner></TopBanner>
      <Navbar></Navbar>
    </header>
  );
};
